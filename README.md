# alexandria
A robust, Java-based command-line catalog for managing technical materials. It uses a Repository layer for clean CRUD operations and MySQL to persist all metadata (titles, links). This layered architecture ensures scalability, allowing for easy migration to any modern front-end.
